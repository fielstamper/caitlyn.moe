---
import Card from "./Card.astro";

const { title, href } = Astro.props;
const currentPage = Astro.url.pathname;
---

<Card
	as="a"
	href={href}
	draggable={false}
	variant={currentPage == href ? "accented" : "bordered"}
>
	{title}
</Card>
